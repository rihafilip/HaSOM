Expressions = (
    foo = (
        | ret first last |

        first := 1.
        last := 100 - 1.
        first := last := last - first.

        ret := first.
        ret := ret := #foo.

        ^ ret asString + first.
    )
)
